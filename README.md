# Prism v2

Self-hosted AI Agent orchestration platform with built-in Harness Runtime governance layer.

Users interact via Web / IM; agents run TAOR (Think → Act → Observe → Respond) loops
in isolated executor sub-processes, streaming results back through Redis and SSE.

See `PRD_V4/` for full specification and `CLAUDE.md` for development guidelines.
