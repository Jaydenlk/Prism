# =============================================================================
# 宿主机 nginx 分流配置
# Prism 跑在 8080，另一个项目跑在 8081（自行修改）
# 安装: apt install nginx && cp deploy-nginx.conf /etc/nginx/conf.d/prism.conf
# =============================================================================

# Prism — 端口 80
server {
    listen 80;
    server_name _;

    client_max_body_size 20M;

    # SSE 长连接（Prism agent 流式响应）
    location ~ ^/api/v1/sessions/[^/]+/stream {
        proxy_pass         http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header   Connection "";
        proxy_buffering    off;
        proxy_cache        off;
        proxy_set_header   X-Accel-Buffering no;
        proxy_read_timeout 3600s;
        chunked_transfer_encoding off;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # 所有请求转发到 Prism（Docker 内部 nginx:8080）
    location / {
        proxy_pass         http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

# 另一个项目 — 端口 8081（改成你的实际端口）
# server {
#     listen 8081;
#     server_name _;
#
#     location / {
#         proxy_pass http://127.0.0.1:你的项目端口;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }
