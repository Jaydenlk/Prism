# PRD v3.0 → v3.1 修订实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按照 `docs/superpowers/specs/2026-04-05-prism-v2-prd-v3.1-revision-design.md` 修订全部 13 个 PRD 文档 (DOC-00~12)，整合 39 项审计修补 + 7 个新 Task + ADR 全局重编号 + 测试/开发标准升级。

**Architecture:** 逐文档修订，每个 Task 对应一个 DOC 文件的全部修改。文档间有 ADR 编号依赖（DOC-05 重编号后影响 DOC-06/07/12），因此 DOC-05 需在 DOC-06/07/12 之前完成。

**Tech Stack:** Markdown 文档编辑

**Spec 文件路径:** `docs/superpowers/specs/2026-04-05-prism-v2-prd-v3.1-revision-design.md`（以下简称"Spec"）

---

## 文件清单

| DOC | 实际文件名 | 操作 |
|-----|----------|------|
| DOC-00 | `PRD/DOC-00-v3-merged.md` | 修改: §10 大幅扩充 |
| DOC-01 | `PRD/DOC-01-v3-merged.md` | 修改: §3.1, §6, §7.1, §9.1 |
| DOC-02 | `PRD/DOC-02-v3-merged.md` | 修改: Task 2.3 ENCRYPTION_KEY, Task 2.2/2.4 |
| DOC-03 | `PRD/DOC-03-Agent-Runtime-and-Harness-Core.md` | 修改: 新增 Task 3.6 |
| DOC-04 | `PRD/DOC-04-Agent-Orchestration.md` | 修改: 新增 Task 4.5, Task 4.2/4.4 |
| DOC-05 | `PRD/DOC-05-Plugin-Ecosystem.md` | 修改: 重构 + 新增 Task 5.5~5.7 + ADR 重编号 |
| DOC-06 | `PRD/DOC-06-Backend-Auth-and-User.md` | 修改: ADR 重编号 |
| DOC-07 | `PRD/DOC-07-Backend-Session-Run-Task.md` | 修改: ADR 重编号 + sync/async |
| DOC-08 | `PRD/DOC-08-Backend-IM-Gateway.md` | 修改: send_run_result + 配对码 |
| DOC-09 | `PRD/DOC-09-Backend-MCP-Provider-Admin.md` | 修改: 新增 Task 9.3 + MCP user_id |
| DOC-10 | `PRD/DOC-10-Frontend-Foundation.md` | 修改: SSE 事件数修正 |
| DOC-11 | `PRD/DOC-11-Frontend-Features.md` | 修改: 新增 Task 11.5 + 规格补充 |
| DOC-12 | `PRD/DOC-12-Observability-and-Entropy.md` | 修改: Entropy 窗口 + ADR 重编号 |
| AUDIT | `PRD/DOC-AUDIT-00-04.md` | 修改: 标记已整合 |

---

### Task 1: DOC-00 — 测试与开发标准升级

**Files:**
- Modify: `PRD/DOC-00-v3-merged.md`

**修改概览:** 版本号 3.0→3.1，§10.1 强化为六原则，§10.4 验收流程升级，§10.5 改为推荐，新增 §10.8 测试规范 + §10.9 开发六原则 + §10.10 验收流程升级

- [ ] **Step 1: 版本号升级**

找到第 4 行：
```
> **版本**: 3.0（Harness-Native 融合版）
```
替换为：
```
> **版本**: 3.1（Harness-Native 融合版）
```

- [ ] **Step 2: §10.4 验收流程升级**

找到 §10.4 中的验收流程代码块（第 605~615 行），将整个代码块替换为：

```
1. 编译通过 — Python: mypy/pyright 类型检查，TypeScript: npx tsc --noEmit 零错误
2. 后端非 AI 接口 — pytest: 正常结果 + 异常结果 + 数据隔离测试
3. 后端 AI 接口 — pytest: 复杂场景决策能力测试 + 执行能力测试 + 边界场景测试
4. 前端 — Playwright E2E: 桌面(1280x800) + 移动(375x812)，正常流程 + 边界场景
5. 文档更新 — PROGRESS.md 状态更新、DECISIONS.md 新增 ADR（如有技术决策）
6. 代码规范 — UUIDv7、ApiResponse<T>、丰富注释、无魔法数字
7. 代码质量审查 — 复用性、命名规范、效率（推荐使用 Simplify skill 如可用）
8. lint + build + 逻辑验证（推荐使用 PJR skill 如可用）
9. 合并 — git merge to dev 流程
```

步骤 7 和 8 不再硬依赖外部 Skill，改为描述行为目标并注明推荐工具。

- [ ] **Step 3: §10.5 Skill 加载规则改为推荐**

找到第 617~627 行的 §10.5 节，将标题和引导文字修改：

将：
```
### 10.5 Skill 加载规则

以下 Skill 在对应场景下**必须加载**，找不到立即停止任务上报：
```
替换为：
```
### 10.5 Skill 加载规则

以下 Skill 在对应场景下**推荐加载**。如果 Skill 不可用，执行等效的手动审查步骤：
```

表格标题从"必须加载的 Skill"改为"推荐加载的 Skill"：

将表头中的：
```
| 场景 | 必须加载的 Skill |
```
替换为：
```
| 场景 | 推荐加载的 Skill |
```

- [ ] **Step 4: 新增 §10.8 测试规范**

在 §10.7 结尾（第 649 行 `- 构建产物（standalone）体积目标：< 150MB` 之后，第 650 行 `---` 之前）插入以下完整内容：

```markdown

### 10.8 测试规范

**测试框架选型**：

| 层 | 框架 | 用途 |
|----|------|------|
| 后端单元测试 | pytest + pytest-asyncio | Service 层、核心模块 |
| 后端 Mock | respx（HTTP mock）+ pytest-httpx | 模型 API 调用 mock |
| 后端 DB 测试 | test database + transaction rollback | 每个测试用例自动回滚 |
| 前端 E2E | Playwright | 桌面(1280x800) + 移动(375x812) 双视口 |
| CI | pytest -x + npx playwright test | 每个 Task 完成时必须全过 |

**后端测试 — 非 AI 接口**：

所有 CRUD、Auth、Provider、MCP、IM 等非 AI 接口必须覆盖：

- **正常结果测试**：标准输入 → 预期输出，验证 HTTP 状态码 + 响应体结构 + 数据持久化
- **异常结果测试**：
  - 401: 无 token / 过期 token / 无效 token
  - 403: 角色权限不足（user 调用 admin 接口）
  - 404: 资源不存在
  - 409: 唯一约束冲突（重复邮箱、重复邀请码）
  - 422: 参数校验失败（格式错误、缺失必填字段）
- **数据隔离测试**：用户 A 不能访问用户 B 的数据（铁律 4 验证）

覆盖率要求：核心模块（TAOR 循环、Middleware Pipeline、Hook System、Permission Engine）≥ 80%。

**后端测试 — AI 接口**：

涉及 Agent 执行链路的接口（POST /tasks → Harness → Agent → Callback）需要专项测试：

- **决策能力测试**：构造复杂场景任务，验证提示词 + 上下文 + 约束条件能否引导 Agent 做出正确决策
  - 场景示例：多步骤任务自动路由到 Coordinator 模式
  - 场景示例：检测到危险操作时触发权限询问
  - 场景示例：Research Agent 不执行写操作
  - Mock 策略：使用 respx mock 模型 API，返回预设的模型响应序列
- **执行能力测试**：工具执行后的结果是否符合预期
  - 场景示例：工具结果超限自动截断
  - 场景示例：工具连续失败触发 Circuit Breaker
  - 场景示例：上下文溢出触发 Compaction
- **边界场景测试**：
  - 循环检测触发（重复工具调用）
  - Guardrail 拦截（违规内容检测）
  - Fork 深度限制触发
  - max_turns 达到上限
  - 模型 API 超时 / 错误码处理

**前端测试 — Playwright E2E**：

每个前端 Task 必须包含 Playwright 测试，双视口同时验证：

- **正常流程测试**：完整业务链路走通
  - 示例：登录 → 创建会话 → 发送消息 → 等待 Agent 流式响应 → 工具卡片展示 → Harness 通知展示 → Run 完成
  - 示例：设置页 → 添加 Provider → 测试连通性 → 设为默认
- **边界场景测试**：预期异常的正确拦截
  - 示例：无效 token → 重定向登录页
  - 示例：SSE 连接断开 → 自动重连 + 恢复消息
  - 示例：超长消息 → 正确渲染不溢出
  - 示例：网络离线 → 错误提示 + 恢复后重试

**模块级任务测试**：

如果一个 Task 同时涉及前端和后端，则前后端测试都必须执行。

### 10.9 开发六原则（强化版）

> 本节为 §10.1 的强化详述版，§10.1 为精简列表，本节为完整规范。

1. **单一职责原则** — 每个服务、类、方法只负责一个明确的职责域，避免职责混乱
2. **最简代码原则** — 不做向后兼容，宁愿破坏性更新也要保证代码最简化，删除所有冗余代码
3. **类型严格原则** — TypeScript 不使用 `any`，Python 使用完整的 type hints，编译/类型检查错误必须立即修复
4. **KISS 原则** — 保持简单直接，如果需要解释就是太复杂了
5. **文档置信度原则** — 绝不基于推测写代码，涉及关键功能时文档置信度不高必须停止并要求准确资料
6. **禁止打补丁原则** — 所有修改必须深度融入代码逻辑，通过重构或调整现有逻辑的方式实现。严禁不从根源解决问题而用补丁绕过。代码过程可以复杂，但最终结果必须是最简洁且完整实现需求的形态

### 10.10 验收流程补充说明

步骤 7 和 8（§10.4）中的 Simplify 和 PJR 为推荐工具。若不可用，则执行等效的手动审查：

- 步骤 7 等效操作：逐文件审查代码复用性、命名一致性、算法效率
- 步骤 8 等效操作：手动运行 `ruff check .`（Python）/ `npx eslint .`（TS）+ `npx tsc --noEmit` + 逻辑走查
```

- [ ] **Step 5: 验证**

确认 §10 下现在包含 §10.1~10.10 共 10 个子节。确认版本号为 3.1。

- [ ] **Step 6: Commit**

```bash
git add PRD/DOC-00-v3-merged.md
git commit -m "docs(DOC-00): v3.1 — add testing spec §10.8, dev principles §10.9, upgrade acceptance flow"
```

---

### Task 2: DOC-01 — API 路由表 + 审计修补

**Files:**
- Modify: `PRD/DOC-01-v3-merged.md`

**修改概览:** 版本号升级，§3.1 补充 sync/async 分界说明 (P0-2)，§6.10 补充 Harness 路由，新增 §6.12 Skills Market 路由，§7.1 harness_event 子类型完善 (P1-18)，§9.1 补充说明 (P0-2)，§4.2 harness_summary schema 示例 (P1-3)

- [ ] **Step 1: 版本号升级**

找到第 4 行：
```
> **版本**: 3.0（Harness-Native 融合版）
```
替换为：
```
> **版本**: 3.1（Harness-Native 融合版）
```

- [ ] **Step 2: §3.1 补充 sync/async 分界说明 (P0-2)**

在 §3.1 任务执行流程的代码块之后（第 204 行 ``` 之后），插入：

```markdown

> **Sync/Async 分界**：Backend（FastAPI）层使用 async def 处理 HTTP 请求，DB 操作使用 SQLAlchemy 2.0 sync Session（在 async 路由中通过 `run_in_executor` 或独立线程运行同步 DB 调用）。Executor（CLI 子进程）内部使用同步执行模型（TAOR 主循环为同步 while loop），仅在 MCP 通信和模型 API 调用时使用 async。Executor 运行时**不直接访问 DB**，所有持久化通过 HTTP 回调委托给 Backend。
```

- [ ] **Step 3: §4.2 harness_summary 补充示例 (P1-3)**

在 `harness_summary` 字段定义附近（§4.2 表清单中 `agent_runs` 表的 `harness_summary` 字段后），插入示例 JSON：

```markdown

> `harness_summary` JSONB 示例：
> ```json
> {
>   "guardrail_triggers": [{"rule_id": "GR-001", "action": "block", "tool": "bash", "reason": "检测到 rm -rf"}],
>   "permission_denials": [{"tool": "file_delete", "decision": "deny"}],
>   "loop_detections": 0,
>   "compaction_level": 2,
>   "middleware_stats": {"loop_detection": {"checks": 15}, "rate_limit": {"throttled": 0}},
>   "total_tool_calls": 8,
>   "total_turns": 12
> }
> ```
```

- [ ] **Step 4: §6.10 Harness 路由补充**

找到 §6.10 的路由表（第 656~660 行），替换为完整版本：

```markdown
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /harness/status | Harness 运行时状态（活跃中间件、熔断器状态、护栏规则数） |
| GET | /harness/traces | Harness 审计轨迹查询（from audit_logs where action like 'harness.%'） |
| GET | /runs/{id}/harness-summary | 指定 Run 的 Harness 执行摘要 |
| GET | /harness/config | 当前有效配置（含 source_trace）— admin only |
| PATCH | /harness/config | 更新配置（写入 DB）— admin only |
| POST | /harness/config/reload | 强制重载所有配置源 — admin only |
| GET | /harness/analytics | Harness 数据聚合 — admin only |
| POST | /harness/entropy-check | 触发熵检测 — admin only |
```

- [ ] **Step 5: 新增 §6.12 Skills Market 路由**

在 §6.11 内部接口（第 662 行）之前，插入新节：

```markdown

### 6.12 Skills Market (DOC-05)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /skills/search | 跨源搜索 Skills（?q=...&source=...） | bearer |
| GET | /skills/installed | 已安装 Skills 列表 | bearer |
| POST | /skills/install | 安装 Skill（{package_id, source, version?}） | bearer |
| DELETE | /skills/{name} | 卸载 Skill | bearer |
| POST | /skills/{name}/update | 更新 Skill | bearer |
| GET | /skills/{name} | Skill 详情 | bearer |

```

- [ ] **Step 6: §7.1 harness_event 子类型补充 (P1-18)**

找到 §7.1 中 `harness_event` 行（第 685 行），将 subtype 列表从 4 种扩展为完整 8 种：

将：
```
| `harness_event` ⚡新增 | Harness 治理事件 | `{"type": "guardrail_trigger"\|"permission_deny"\|"loop_detected"\|"compaction", "detail": {...}}` |
```
替换为：
```
| `harness_event` ⚡新增 | Harness 治理事件 | `{"type": "guardrail_trigger"\|"permission_deny"\|"permission_ask"\|"loop_detected"\|"compaction"\|"circuit_break"\|"feedback_alert"\|"middleware_action", "detail": {...}}` |
```

- [ ] **Step 7: §9.1 补充 Executor 不直接访问 DB (P0-2)**

在 §9.1 的 "Backend 收到回调后" 说明（第 897 行）之后，插入：

```markdown

> **关键约束**：Executor（CLI 子进程）运行时**不直接访问 DB**。所有持久化、状态更新、队列推进均通过 HTTP 回调委托给 Backend。这保证了 Executor 可以独立伸缩且不会产生 DB 连接竞争。
```

- [ ] **Step 8: 目录更新**

在 §6 目录列表中（第 12~27 行的目录节），确保新增的 §6.12 出现在目录中。

- [ ] **Step 9: Commit**

```bash
git add PRD/DOC-01-v3-merged.md
git commit -m "docs(DOC-01): v3.1 — add Skills Market routes §6.12, supplement Harness routes, fix sync/async boundary"
```

---

### Task 3: DOC-02 — 审计修补

**Files:**
- Modify: `PRD/DOC-02-v3-merged.md`

**修改概览:** 版本号升级，Task 2.3 ENCRYPTION_KEY 独立于 JWT_SECRET (P0-3)，Task 2.2 Stream 容错 (P1-5)，Task 2.4 工具 cache 失效 (P1-6)

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: Task 2.3 ENCRYPTION_KEY (P0-3)**

在 Task 2.3 Part A 的设计决策部分，找到涉及 API Key 加密的内容，补充说明：

```markdown

> **安全修正 (P0)**：API Key 加密密钥 `ENCRYPTION_KEY` 必须独立于 `JWT_SECRET`。JWT_SECRET 用于 token 签名，ENCRYPTION_KEY 用于 Provider API Key 的 AES 加密存储。两者职责不同，不可共用。在 `app/core/config.py` 中新增 `ENCRYPTION_KEY: str` 环境变量，ProviderService 的加解密方法改用 `ENCRYPTION_KEY`。
```

同时在 Task 2.3 Part B 的 Claude Code 执行 Prompt 中，找到 `config.py` 相关部分，确保 ENCRYPTION_KEY 作为独立环境变量出现（与 JWT_SECRET 分开）。

- [ ] **Step 3: Task 2.2 Stream 容错 (P1-5)**

在 Task 2.2 Part A 中补充：

```markdown

> **Stream JSON 解析容错**：当模型返回的 SSE 数据行 JSON 格式异常时（截断、多余逗号等），先尝试 `json_repair` 库修复，仍失败则发送 `StreamEvent(type="error", data={"raw": raw_line, "error": "json_parse_failed"})` 并跳过该行，不中断整个 stream。
```

- [ ] **Step 4: Task 2.4 工具列表 cache 失效 (P1-6)**

在 Task 2.4 Part A 中补充：

```markdown

> **工具列表 Cache 失效**：PromptAssembler 缓存的工具列表通过 `tools_hash`（对所有已注册工具名 + 版本的 SHA256）校验。每次 `build()` 前比较 hash，如果 PluginHost 有工具变更（插件加载/卸载），cache 自动失效重建。
```

- [ ] **Step 5: Commit**

```bash
git add PRD/DOC-02-v3-merged.md
git commit -m "docs(DOC-02): v3.1 — fix ENCRYPTION_KEY separation (P0), add stream json repair, tools cache invalidation"
```

---

### Task 4: DOC-03 — Harness 动态更新 (Task 3.6) + 审计修补

**Files:**
- Modify: `PRD/DOC-03-Agent-Runtime-and-Harness-Core.md`

**修改概览:** 版本号升级，文档开头增加 Task 间依赖 (P1-7)，Task 3.3 Hook 事件分 Phase (P1-8)，Task 3.5 Memory 分 Phase (P1-9)，文件末尾新增 Task 3.6 全文

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: 新增 Task 间最小可行依赖 (P1-7)**

在目录节（第 13 行 `## 目录`）之后、第一个 Task（第 23 行 `## Task 3.1`）之前，插入：

```markdown

## Task 间最小可行依赖

```
Task 3.1 (TAOR 主循环)
  └─► Task 3.2 (Middleware Pipeline) — 依赖 TAOR 的 pre_turn/post_turn 钩点
        └─► Task 3.3 (Hook System) — 依赖 Middleware 的事件分发
        └─► Task 3.4 (Guardrails) — 依赖 Middleware 的 pre/post 拦截点
              └─► Task 3.5 (Compaction) — 依赖 Guardrails 的上下文评估
                    └─► Task 3.6 (Harness 动态更新) — 依赖所有子系统的运行时接口
```

> 最小可行路径：3.1 → 3.2 → (3.3 + 3.4 并行) → 3.5 → 3.6
```

- [ ] **Step 3: Task 3.3 Hook 事件分 Phase (P1-8)**

在 Task 3.3 Part A 中，找到 21 个 Hook 事件的列表，补充分 Phase 说明：

```markdown

> **Phase 分期**：Phase 1 实现以下 8 个核心事件（覆盖完整的工具执行和会话生命周期）：
> `PreToolUse`, `PostToolUse`, `SessionStart`, `SessionEnd`, `PreModelCall`, `PostModelCall`, `CompactionTrigger`, `ErrorOccurred`
>
> 其余事件（如 `ForkStart`, `ForkEnd`, `PlanStepStart` 等）在 Phase 1 中保留枚举定义和类型声明但不实现分发逻辑，Phase 2 按需开启。
```

- [ ] **Step 4: Task 3.5 Memory 分 Phase (P1-9)**

在 Task 3.5 Part A 中，找到 6 层 Memory 的描述，补充分 Phase 说明：

```markdown

> **Phase 分期**：Phase 1 先实现 2 层 Memory：
> 1. **Session Memory**（会话级上下文）— 必需，TAOR 循环的基础
> 2. **User Memory**（用户级偏好/历史）— 跨会话延续性的基础
>
> 其余 4 层（Project Memory, Tool Memory, Agent Memory, Global Memory）在 Phase 1 中预留接口定义（`MemoryLayer` 抽象基类 + `MemoryManager.get_layer()` 方法签名），Phase 2 按需实现。
```

- [ ] **Step 5: 新增 Task 3.6 全文**

在文件末尾（最后一个 Task 3.5 的"完成后"节之后），追加 Task 3.6 的完整内容。

内容来源：Spec §3（DOC-03: Harness 动态更新），包含 §3.1~3.7 全部内容。

追加以下完整内容：

```markdown

---

## Task 3.6: Harness 垂类配置动态更新

### Part A — 设计与解释

#### 问题陈述

Harness 的底层框架（TAOR 循环、Middleware Pipeline 分发机制、Hook 事件分发器等）是代码级组件，变更需要重启。但垂类规则（Guardrail 规则集、Permission 策略、Middleware 启停配置、Hook 注册表、Agent 行为约束等）需要频繁迭代，要求运行时热更新能力。

#### 设计原则

**底层固化 + 垂类热更新**：

| 层级 | 包含内容 | 更新方式 | 理由 |
|------|---------|---------|------|
| **底层（固化）** | TAOR 主循环框架、MiddlewarePipeline 分发机制、HookSystem 事件分发器、PermissionEngine 决策框架、CircuitBreaker 熔断框架、CompactionPipeline 四级策略框架 | 代码更新 + 重启 | 通用性强，改动易出问题，稳定性优先 |
| **垂类层（动态）** | Guardrail 规则集、Permission 策略集（允许/拒绝/询问规则）、Middleware 启停配置及参数调优、Hook 注册表（新增/移除 handler）、Agent 行为约束（各类型约束增减） | 配置更新，运行时热生效 | 垂类影响大，频繁迭代，需要灵活性 |

#### HarnessConfigManager

```python
"""
executor/harness/config_manager.py

Harness 垂类配置运行时管理器。

配置来源（优先级从低到高）：
1. platform_rules.py 硬编码（平台级底线，不可被覆盖）
2. harness_config.yaml 文件（部署时配置）
3. DB 存储的配置（Web UI / API 修改）
4. Plugin 注入的配置（插件加载时动态注入，随插件卸载移除）

热更新触发机制：
- 文件变更：watchdog 监听 harness_config.yaml，检测到变更自动 reload
- DB 变更：API 调用 PATCH /harness/config 后主动 reload
- Plugin 变更：PluginHost 加载/卸载时回调 reload
- 跨进程同步：通过 Redis pub/sub channel `harness:config_reload` 通知其他 CLI 子进程

变更不中断原则：
- reload 操作不中断正在执行的 Run
- 新配置在下一个 TAOR turn 开始时生效（turn 粒度的原子切换）
- 如果 reload 失败（配置格式错误），保持旧配置并写入 audit_logs 告警
"""

@dataclass
class HarnessEffectiveConfig:
    """合并后的最终有效配置"""
    guardrail_rules: list[GuardrailRule]
    permission_policies: dict[str, str]  # tool_name → "allow" | "deny" | "ask_user"
    middleware_config: dict[str, MiddlewareConfig]  # mw_name → {enabled, params}
    hook_registrations: list[HookRegistration]
    agent_constraints: dict[str, list[str]]  # agent_type → [constraint_text]
    source_trace: dict[str, str]  # 每条配置的来源标注（debug 用）


class HarnessConfigManager:
    def __init__(
        self,
        config_file_path: str,
        db_session_factory,
        redis_client,
        platform_rules: list,
    ):
        self._effective: HarnessEffectiveConfig | None = None
        ...

    def reload(self) -> HarnessEffectiveConfig:
        """
        重新计算有效配置。
        合并策略：高优先级源覆盖低优先级源，但平台级规则不可被覆盖。
        每条配置标注来源（source_trace）。
        """
        ...

    def reload_guardrails(self) -> None:
        """仅重新加载 Guardrail 规则集"""
        ...

    def reload_permissions(self) -> None:
        """仅重新加载权限策略"""
        ...

    def toggle_middleware(self, name: str, enabled: bool) -> None:
        """启用/禁用指定 Middleware（P7 可撕裂原则的运行时体现）"""
        ...

    def get_effective_config(self) -> HarnessEffectiveConfig:
        """获取当前有效配置"""
        if self._effective is None:
            self._effective = self.reload()
        return self._effective

    def inject_plugin_config(self, plugin_name: str, overrides: dict) -> None:
        """插件加载时注入配置"""
        ...

    def remove_plugin_config(self, plugin_name: str) -> None:
        """插件卸载时移除配置"""
        ...
```

#### 配置文件格式

```yaml
# .prism/harness_config.yaml — 运行时热更新
version: "1.0"

guardrails:
  custom_rules:
    - id: GR-CUSTOM-001
      trigger: pre_tool_use
      condition:
        tool_name: bash
        input_contains: ["rm -rf", "DROP TABLE", "DELETE FROM"]
      action: block
      message: "检测到破坏性操作，已拦截"

middlewares:
  loop_detection:
    enabled: true
    params:
      window_size: 5
      max_identical: 3
  rate_limit:
    enabled: true
    params:
      max_tool_calls_per_minute: 30
      max_tokens_per_minute: 100000

permissions:
  default_mode: balanced
  tool_overrides:
    bash: ask_user
    file_write: ask_user
    file_delete: deny
    web_search: allow
    skill_install: ask_user

agent_constraints:
  research:
    - "你是只读探索者。绝对不能创建、修改、删除任何文件或数据。"
  planner:
    - "你是规划者。只输出 step-by-step 计划，不执行任何操作。"
  plugin_builder:
    - "严禁一键生成。必须进入多轮需求收集流程。"
```

#### 与现有子系统的集成

| 子系统 | 当前实现 | 修改后 |
|--------|---------|--------|
| GuardrailsEngine | `platform_rules.py` 硬编码 + Plugin 注入 | 从 `HarnessConfigManager.get_effective_config().guardrail_rules` 读取。平台级规则仍硬编码且不可覆盖 |
| PermissionEngine | 静态配置 | 从配置读取 `default_mode` + `tool_overrides` |
| MiddlewarePipeline | 启动时固定注册 | 启动时从配置加载 + `toggle_middleware()` 运行时增减 |
| HookSystem | settings + Plugin frontmatter | 新增从 `harness_config.yaml` 读取的 Hook 注册 |
| Agent 行为约束 | `prompt_sections.py` 硬编码 | 合并硬编码约束 + 配置中的自定义约束 |

#### API 端点

```
GET    /harness/config          — 获取当前有效配置（含 source_trace）
PATCH  /harness/config          — 更新配置（admin only，写入 DB）
POST   /harness/config/reload   — 强制重载所有配置源（admin only）
```

#### 设计决策

- **ADR-030**: Harness 底层/垂类分层架构 — 底层框架代码级固化，垂类规则配置级热更新，turn 粒度原子切换

#### 验收标准

- `harness_config.yaml` 修改后，下一个 TAOR turn 自动使用新配置
- `PATCH /harness/config` 修改 Guardrail 规则后，新规则立即生效（下一个 Run）
- `toggle_middleware("loop_detection", false)` 后，循环检测不再执行
- 平台级规则（铁律相关的 Guardrail）不可被任何配置源覆盖
- 配置格式错误时不 crash，保持旧配置并写入 audit_logs
- 跨进程同步：修改配置后，所有正在运行的 CLI 子进程在下一个 turn 使用新配置

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充（本 Task 的 Part B 将在代码实现阶段由 AI 生成完整的执行 Prompt）

## 完成后

1. `harness_config.yaml` 可被 Harness Runtime 读取并应用
2. HarnessConfigManager 的 4 源优先级合并逻辑正常工作
3. 更新 DECISIONS.md：记录 ADR-030（Harness 底层/垂类分层架构）
4. 更新 PROGRESS.md：Task 3.6 状态标记为 done
```

- [ ] **Step 6: 目录更新**

在文件开头的目录节中，确保 Task 3.6 出现在目录列表中。

- [ ] **Step 7: Commit**

```bash
git add PRD/DOC-03-Agent-Runtime-and-Harness-Core.md
git commit -m "docs(DOC-03): v3.1 — add Task 3.6 Harness dynamic config, add task dependencies, phase Hook/Memory"
```

---

### Task 5: DOC-04 — PluginBuilder Agent (Task 4.5) + 审计修补

**Files:**
- Modify: `PRD/DOC-04-Agent-Orchestration.md`

**修改概览:** 版本号升级，Task 4.2 补充 Fork 深度限制 (P0-4)，Task 4.3 补充 parse 重试 (P1-10)，Task 4.4 补充英文关键词 (P1-22)，新增 Task 4.5 PluginBuilder Agent

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: Task 4.2 Fork 深度限制 (P0-4)**

在 Task 4.2 Part A 中，找到 ForkManager 的设计部分，补充：

```markdown

> **Fork 深度限制 (P0)**：`ForkManager` 增加 `max_fork_depth: int = 2` 参数。Fork 内再 Fork 时深度递增，达到 `max_fork_depth` 时返回错误 `ForkDepthExceeded("最大 Fork 深度为 {max_fork_depth}")` 而非继续嵌套。防止无限递归 Fork 消耗资源。
```

同时在 Part B 的 ForkManager 代码中确保 `max_fork_depth` 参数存在。

- [ ] **Step 3: Task 4.2 Fork 超时 (P2-5)**

在 Task 4.2 Part A 补充：

```markdown

> **Fork 超时**：`ForkManager.fork()` 增加 `fork_timeout: int = 300`（秒）参数。子 Agent 执行超过此时间强制终止并返回超时错误。
```

- [ ] **Step 4: Task 4.3 Plan.parse_from_text 重试 (P1-10)**

在 Task 4.3 Part A 中，找到 `Plan.parse_from_text()` 相关描述，补充：

```markdown

> **解析可靠性**：`Plan.parse_from_text()` 采用两级策略：
> 1. 优先使用 structured output（要求模型返回 JSON 格式的计划）
> 2. 如果模型不支持 structured output，回退到正则解析 + 一次 retry（将解析失败的结果连同原始文本重新发送给模型要求修正格式）
```

- [ ] **Step 5: Task 4.4 TaskRouter 补充英文关键词 (P1-22)**

在 Task 4.4 Part A/B 中，找到 `COORDINATOR_PATTERNS` 和 `AGENT_TYPE_PATTERNS` 的定义，为所有模式补充英文关键词。例如：

在 COORDINATOR_PATTERNS 中添加：
```python
# 英文关键词（新增）
r"(analyze|research|compare).{0,10}(and|then).{0,10}(implement|build|create)",
r"(step by step|multi-step|complex).{0,10}(task|project|workflow)",
```

在 AGENT_TYPE_PATTERNS 中为 research/planner/verifier 各补充英文模式。

- [ ] **Step 6: 新增 Task 4.5 PluginBuilder Agent**

在文件末尾追加 Task 4.5 全文。内容来源：Spec §4（DOC-04: PluginBuilder Agent），包含 §4.1~4.6。

追加完整内容（包含 AgentDefinition、多轮对话流程、PluginBuilderGate Middleware、GuardrailRule、TaskRouter 扩展、验收标准）。内容与 Spec §4 一致，格式适配为 DOC-04 的 Part A / Part B 结构：

```markdown

---

## Task 4.5: PluginBuilder Agent 与多轮对话流程

### Part A — 设计与解释

#### 问题陈述

插件创建是高复杂度任务，需要充分理解用户需求后才能生成高质量插件。一键生成的插件质量无法保证。需要一个专用 Agent + Harness 双保险机制，强制多轮需求收集对话。

#### Agent 定义

```python
# executor/agents/plugin_builder.py

PLUGIN_BUILDER = AgentDefinition(
    name="plugin_builder",
    display_name="插件构建器",
    system_prompt_override=(
        "你是 Prism 插件构建专家。你的工作是通过多轮对话充分理解用户的插件需求，"
        "然后设计并生成符合 Prism 插件规范（CC 兼容格式）的完整插件。\n\n"
        "你绝对不能一键生成插件。你必须按照以下流程工作：\n"
        "1. 需求收集阶段（5-8 轮对话，每轮最多 5 个问题）\n"
        "2. 设计方案展示阶段（展示完整设计，等待用户确认）\n"
        "3. 生成执行阶段（按确认的设计逐个生成文件）\n"
        "4. 验证阶段（加载测试 + 结果汇报）\n\n"
        "每个阶段必须获得用户明确确认才能推进。"
    ),
    allowed_tools=[
        "file_read", "file_write", "bash",
        "web_search", "skill_install",
    ],
    denied_tools=[],
    read_only=False,
    behavior_constraints=[
        "严禁一键生成。检测到插件创建意图后，必须进入多轮需求收集流程。",
        "需求收集阶段：最少 5 轮、最多 8 轮对话，每轮最多问 5 个问题。",
        "目标：最大化清晰所有模糊内容和可能的边界情况。",
        "每个阶段必须获得用户明确的确认才能推进到下一阶段。",
        "生成的插件必须符合 Prism 插件规范（CC 兼容格式）。",
        "生成完成后必须自动运行加载测试验证插件可用性。",
    ],
    output_format="structured_dialogue",
    max_turns=50,
)
```

#### 多轮对话流程

```
阶段 1: 需求收集（5-8 轮，每轮 ≤5 问题）
│
├── 第 1 轮：插件目标和核心使用场景
├── 第 2 轮：能力需求（Skills、MCP、Hook）
├── 第 3 轮：行为约束与安全
├── 第 4 轮：边界场景与异常处理
├── 第 5 轮：细节确认与补充
├── [可选] 第 6-8 轮：深度探讨
└── Harness 门控：轮数 < 5 时阻止进入阶段 2

阶段 2: 设计方案展示 → 等待用户确认
阶段 3: 生成执行 → 创建插件文件
阶段 4: 验证 → 加载测试 + 结果汇报
```

#### Harness 双保险

**保险 1：PluginBuilderGate Middleware**

```python
# executor/harness/middleware/plugin_builder_gate.py

class PluginBuilderGate(Middleware):
    """仅对 PluginBuilder Agent 生效的阶段门控。"""
    MIN_COLLECTION_ROUNDS = 5
    MAX_COLLECTION_ROUNDS = 8
    MAX_QUESTIONS_PER_ROUND = 5

    def pre_turn(self, context: MiddlewareContext) -> MiddlewareContext:
        if context.agent_type != "plugin_builder":
            return context
        phase = context.metadata.get("plugin_build_phase", 1)
        turn_count = context.metadata.get("phase_turn_count", 0)
        if phase == 1 and turn_count < self.MIN_COLLECTION_ROUNDS:
            context.inject_constraint(
                f"你仍在需求收集阶段。还需要至少 {self.MIN_COLLECTION_ROUNDS - turn_count} 轮对话。"
            )
        ...
```

**保险 2：GuardrailsEngine 拦截规则**

```python
# 新增 Guardrail 规则（platform_rules.py，不可覆盖）
GR_PLUGIN_CREATE_GUARD = GuardrailRule(
    id="GR-PLUGIN-CREATE-001",
    trigger="pre_tool_use",
    condition=lambda ctx: (
        ctx.tool_name in ["file_write", "bash"] and
        ctx.agent_type != "plugin_builder" and
        _is_plugin_file(ctx.tool_input)
    ),
    action="block",
    message="插件文件的创建/修改必须通过 PluginBuilder Agent 流程完成",
    platform_level=True,
)
```

#### TaskRouter 扩展

```python
PLUGIN_BUILDER_PATTERNS = [
    r"(创建|制作|开发|构建|搭建|生成|做).{0,10}(插件|plugin)",
    r"(帮我|请).{0,10}(做|弄|写|建).{0,10}(插件|plugin|skill)",
    r"(create|build|make|develop|generate).{0,10}(plugin|skill)",
    r"(help me|please).{0,10}(build|create|make).{0,10}(plugin|skill)",
]
```

#### 设计决策

- **ADR-017**: PluginBuilder Agent 多轮对话强制 — 最少 5 轮需求收集 + Harness 阶段门控，防止一键生成低质量插件

#### 验收标准

- 用户说"帮我创建一个金融分析插件" → 自动路由到 PluginBuilder Agent
- Agent 在需求收集阶段提问 5-8 轮，每轮 ≤5 个问题
- 需求收集不足 5 轮时被 Harness 阻止进入设计阶段
- 设计方案展示后必须等待用户确认才进入生成阶段
- 非 PluginBuilder Agent 尝试写 plugin.yaml 时被 Guardrail 拦截
- 生成完成后自动运行加载测试

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充

## 完成后

1. PluginBuilder Agent 可通过 TaskRouter 自动路由
2. 多轮对话流程被 Harness 强制执行
3. 更新 DECISIONS.md：记录 ADR-017（PluginBuilder Agent 多轮对话强制）
4. 更新 PROGRESS.md：Task 4.5 状态标记为 done
```

- [ ] **Step 7: 目录更新**

确保 Task 4.5 出现在文件开头的目录中。

- [ ] **Step 8: Commit**

```bash
git add PRD/DOC-04-Agent-Orchestration.md
git commit -m "docs(DOC-04): v3.1 — add Task 4.5 PluginBuilder Agent, fix Fork depth/timeout, TaskRouter i18n"
```

---

### Task 6: DOC-05 — Skills Market + CC 插件协议 + ADR 重编号

**Files:**
- Modify: `PRD/DOC-05-Plugin-Ecosystem.md`

**修改概览:** 版本号升级，文档重构为三部分，ADR 017→018, 018→019, 019→020, 020→021, 021→022 重编号，Task 5.2 MCP 异步修复 (P0-5)，Task 5.1 Hook 代码取消注释 (P1-11)，新增 Task 5.5~5.7

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: ADR 重编号**

全文替换（注意只替换文档正文中的 ADR 编号，不要替换代码块内的引用）：

| 原编号 | 新编号 |
|--------|--------|
| ADR-017（Skill 三级加载） | ADR-018 |
| ADR-018（MCP cache 失效） | ADR-019 |
| ADR-019（Hook 优先级排序） | ADR-020 |
| ADR-020（Plugin 命名空间） | ADR-021 |
| ADR-021（PluginHost 统一管理） | ADR-022 |

执行全局替换，按从大到小的顺序避免连锁替换：
1. `ADR-021` → `ADR-022`
2. `ADR-020` → `ADR-021`
3. `ADR-019` → `ADR-020`
4. `ADR-018` → `ADR-019`
5. `ADR-017` → `ADR-018`

- [ ] **Step 3: 文档结构重组**

在目录节之后、Task 5.1 之前，插入三部分划分说明：

```markdown

## 文档结构

```
Part I:   Plugin Core（Task 5.1~5.4）— 插件核心加载与管理
Part II:  Skills Market（Task 5.5~5.6）— 多源聚合与安装入口
Part III: CC Plugin Protocol Compatibility（Task 5.7）— CC 插件格式兼容层
```

---

## Part I: Plugin Core

```

在 Task 5.4 "完成后" 节之后，插入 Part II 分隔：

```markdown

---

## Part II: Skills Market

```

- [ ] **Step 4: Task 5.2 MCP 异步修复 (P0-5)**

在 Task 5.2 Part A 中，找到 `MCPClient._send_request()` 或 subprocess 相关描述，补充：

```markdown

> **异步修复 (P0)**：`MCPClient._send_request()` 中的 `subprocess.stdout.readline()` 是阻塞调用，在 async 上下文中会阻塞事件循环。必须改用 `asyncio.create_subprocess_exec()` 创建子进程，通过 `process.stdout.readline()`（async 版本）进行非阻塞读取。
```

同时在 Part B 的代码中，确保所有 subprocess 调用使用 `asyncio.create_subprocess_exec`。

- [ ] **Step 5: Task 5.1 Hook 注册代码取消注释 (P1-11)**

在 Task 5.1 Part B 中，找到被注释的 Hook 注册代码，取消注释并确保完整实现。

- [ ] **Step 6: 新增 Task 5.5 — Skills Registry & Multi-Source Aggregation**

在 Part II 分隔之后，追加 Task 5.5 全文。内容来源：Spec §5.2。

```markdown

## Task 5.5: Skills Registry & Multi-Source Aggregation

### Part A — 设计与解释

#### 问题陈述

当前 Skill 仅支持本地 `.skills/` 目录加载。用户需要一个多源聚合的 Skills 市场，支持从 Manus、npm、GitHub、本地等多个源搜索和安装 Skills。

#### 多源架构

```python
# executor/plugins/skills_registry.py

class SkillSource(ABC):
    """Skills 源抽象基类"""
    @property
    @abstractmethod
    def source_name(self) -> str: ...

    @abstractmethod
    async def search(self, query: str) -> list[SkillPackage]: ...

    @abstractmethod
    async def fetch(self, package_id: str, version: str | None = None) -> SkillBundle: ...

    @abstractmethod
    async def get_versions(self, package_id: str) -> list[str]: ...


class LocalSource(SkillSource):
    """本地 .skills/ + .prism/skills/ 扫描"""
    source_name = "local"

class NpmSource(SkillSource):
    """npm registry 源（通过 npm pack 下载 tarball）"""
    source_name = "npm"

class ManusSource(SkillSource):
    """Manus Skills Market API 源"""
    source_name = "manus"

class GitHubSource(SkillSource):
    """GitHub 仓库直接安装（支持 branch/tag/commit）"""
    source_name = "github"


class SkillsRegistry:
    """Skills 多源聚合注册表"""
    async def search(self, query: str, sources: list[str] | None = None) -> list[SkillPackage]: ...
    async def install(self, package_id: str, source: str, version: str | None = None) -> InstalledSkill: ...
    async def uninstall(self, skill_id: str) -> None: ...
    async def update(self, skill_id: str) -> InstalledSkill: ...
    def list_installed(self) -> list[InstalledSkill]: ...
```

#### 安装目录结构

```
{workspace}/
├── .skills/                          # CC 兼容自动扫描目录
└── .prism/
    └── skills/                       # Prism 管理的已安装 Skills
        ├── registry.json             # 已安装 Skills 索引
        ├── @manus/
        ├── @npm/
        └── @github/
```

#### 设计决策

- **ADR-023**: Skills 多源聚合策略 — 搜索时并行查询 + 合并去重，安装时按用户选择的源

#### 验收标准

- `SkillsRegistry.search("web research")` 返回来自多个源的结果
- `SkillsRegistry.install("@manus/web-researcher", "manus")` 安装成功
- 安装的 Skill 可被 Agent 调用
- 卸载后 Hooks 和 MCP 工具正确注销

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充

## 完成后

1. SkillsRegistry 多源搜索和安装正常工作
2. registry.json 正确维护已安装 Skills 索引
3. 更新 DECISIONS.md：记录 ADR-023（Skills 多源聚合策略）
4. 更新 PROGRESS.md：Task 5.5 状态标记为 done
```

- [ ] **Step 7: 新增 Task 5.6 — Skills CLI & Agent Tool**

紧接 Task 5.5 之后追加：

```markdown

---

## Task 5.6: Skills CLI & Agent Tool

### Part A — 设计与解释

#### 问题陈述

Skills 市场需要三个入口：CLI（管理员）、Web UI（用户）、Agent Tool（AI 自主安装）。本 Task 实现 CLI 和 Agent Tool 两个入口。

#### CLI 命令

```
prism skills search <query> [--source manus|npm|github|local]
prism skills install <package_id> [--source <source>] [--version <ver>]
prism skills uninstall <skill_name>
prism skills update <skill_name>
prism skills list [--source <source>]
prism skills info <skill_name>
```

#### Agent 内置工具

```python
# executor/tools/builtin/skill_install.py

class SkillInstallTool(Tool):
    """
    让 Agent 能搜索和安装 Skills。
    权限约束：search 无需审批，install/uninstall/update 需 PermissionEngine 审批。
    仅 General 和 PluginBuilder Agent 可使用。
    """
    name = "skill_manage"
    description = "搜索、安装和管理 Prism Skills"
```

#### Backend API

```
GET    /skills/search?q=...&source=...     跨源搜索
GET    /skills/installed                    已安装列表
POST   /skills/install                      安装
DELETE /skills/{skill_name}                 卸载
POST   /skills/{skill_name}/update          更新
GET    /skills/{skill_name}                 详情
```

#### 验收标准

- CLI `prism skills search "code review"` 返回结果
- CLI `prism skills install @npm/code-reviewer` 安装成功
- Agent 使用 `skill_manage` 工具搜索并安装 Skills（需用户确认）
- Web UI 通过 Backend API 完成搜索/安装/卸载流程

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充

## 完成后

1. CLI 和 Agent Tool 入口正常工作
2. Backend API 可被前端调用
3. 更新 PROGRESS.md：Task 5.6 状态标记为 done
```

- [ ] **Step 8: 新增 Task 5.7 — CC Plugin Protocol Compatibility**

追加 Part III 分隔和 Task 5.7：

```markdown

---

## Part III: CC Plugin Protocol Compatibility

## Task 5.7: CC 插件格式兼容层

### Part A — 设计与解释

#### 问题陈述

Prism 基于 Claude Code 架构移植开发，需要原生支持 CC 插件格式。Prism 的 plugin.yaml 是 CC plugin.json 的超集，通过 CCPluginAdapter 实现自动检测和转换。

#### 格式映射

```
CC Plugin 目录结构              Prism 映射
──────────────────              ──────────
plugin.json                  → plugin.yaml（超集，自动转换）
skills/                      → skills/（格式一致）
hooks/                       → hooks/（handler 协议一致）
mcp-servers/                 → mcp/（目录名不同，内容兼容）
```

#### plugin.yaml（Prism 超集格式）

```yaml
# CC 兼容字段
name: financial-analysis
description: "金融数据分析插件"
version: "1.0.0"
skills_dir: ./skills
hooks_dir: ./hooks

# Prism 扩展字段（CC 会忽略）
prism:
  vertical_tuning:
    agent_constraints:
      - "所有金融数据引用必须标注来源和时间"
    output_format: "数据表格优先"
    domain_knowledge:
      - "A 股交易时间：9:30-11:30, 13:00-15:00"

  harness_overrides:
    guardrail_rules: [...]
    permissions: {...}

  mcp_servers: [...]
  dependencies:
    skills: ["@manus/web-researcher"]
```

#### CCPluginAdapter

```python
# executor/plugins/cc_compat.py

class CCPluginAdapter:
    def detect_format(self, plugin_dir: str) -> Literal["cc", "prism", "skills_only", "unknown"]: ...
    def load(self, plugin_dir: str) -> PluginConfig: ...
    def export_to_cc(self, config: PluginConfig, output_dir: str) -> None: ...
```

#### PluginHost 集成

```python
class PluginHost:
    def __init__(self, ..., cc_adapter: CCPluginAdapter):
        self._cc_adapter = cc_adapter

    def load_plugin(self, plugin_dir: str) -> None:
        config = self._cc_adapter.load(plugin_dir)  # 统一入口
        ...
```

#### 设计决策

- **ADR-024**: CC 插件协议移植/兼容层 — plugin.yaml 为 CC plugin.json 的超集，自动检测 + 转换

#### 验收标准

- CC 格式插件（含 plugin.json）可被 Prism 直接加载
- Prism 格式插件（plugin.yaml）可加载并应用垂类调教定义
- `export_to_cc()` 可将 Prism 插件导出为 CC 兼容格式
- 只有 skills/ 目录的纯 Skills 集合可作为插件加载

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充

## 完成后

1. CCPluginAdapter 正常工作
2. PluginHost 可加载 CC 和 Prism 两种格式
3. 更新 DECISIONS.md：记录 ADR-024（CC 插件协议移植/兼容层）
4. 更新 PROGRESS.md：Task 5.7 状态标记为 done
```

- [ ] **Step 9: 目录更新**

更新文件开头的目录列表，添加 Task 5.5、5.6、5.7。

- [ ] **Step 10: Commit**

```bash
git add PRD/DOC-05-Plugin-Ecosystem.md
git commit -m "docs(DOC-05): v3.1 — restructure to 3 parts, add Tasks 5.5-5.7 Skills Market + CC compat, renumber ADRs, fix MCP async"
```

---

### Task 7: DOC-06 — ADR 重编号

**Files:**
- Modify: `PRD/DOC-06-Backend-Auth-and-User.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: ADR 重编号**

全文替换：
1. `ADR-018` → `ADR-026`（Refresh token HttpOnly cookie）
2. `ADR-017` → `ADR-025`（ENCRYPTION_KEY 独立）

**重要**：先替换 018 再替换 017，避免连锁替换。

- [ ] **Step 3: Commit**

```bash
git add PRD/DOC-06-Backend-Auth-and-User.md
git commit -m "docs(DOC-06): v3.1 — renumber ADR-017→025, ADR-018→026"
```

---

### Task 8: DOC-07 — ADR 重编号 + 审计修补

**Files:**
- Modify: `PRD/DOC-07-Backend-Session-Run-Task.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: ADR 重编号**

全文替换：
1. `ADR-021` → `ADR-029`（subprocess 调度）
2. `ADR-020` → `ADR-028`（harness_summary schema）
3. `ADR-019` → `ADR-027`（promote 原子性）

先从大编号开始替换。

- [ ] **Step 3: handle_callback sync/async 统一 (P1-12)**

找到 Task 7.3 或相关的 callback 端点代码，确保所有 `def handle_callback` 改为 `async def handle_callback`，并使用 `await` 调用。

```markdown

> **Sync/Async 统一 (P1)**：`handle_callback` 端点统一为 `async def` + `await`。FastAPI 天然支持 async def，无需同步回退。
```

- [ ] **Step 4: complete_and_promote 双 commit 修复 (P1-13)**

找到 Task 7.2 中 `complete_and_promote` 相关代码，补充说明：

```markdown

> **事务一致性 (P1)**：`complete_and_promote()` 中移除端点层（router）的多余 `db.commit()`。所有事务由 Service 层统一管理：`run_service.complete_run()` 内部在同一事务中完成 Run 状态更新 + queue promote + harness_summary 写入，然后统一 commit。
```

- [ ] **Step 5: §4.2 harness_summary promote 原子性补充 (P1-4)**

在 Task 7.2 Part A 的 promote 相关描述中补充：

```markdown

> **Promote 原子性 (P1)**：`session_queue_items` 的 promote 操作必须与 Run 完成在同一 DB 事务中执行（单次 commit）。Backend 启动时检查是否有 `status=running` 但对应子进程已退出的 Run，标记为 failed 并 promote 下一个队列项。
```

- [ ] **Step 6: list_messages limit 上限 (P2-6)**

在 Task 7.1 的 list_messages 相关部分补充：

```markdown

> 查询参数 `limit` 增加上限校验：`max_limit = 500`，超过返回 422。
```

- [ ] **Step 7: Commit**

```bash
git add PRD/DOC-07-Backend-Session-Run-Task.md
git commit -m "docs(DOC-07): v3.1 — renumber ADRs, fix sync/async, promote atomicity, remove double commit"
```

---

### Task 9: DOC-08 — 审计修补

**Files:**
- Modify: `PRD/DOC-08-Backend-IM-Gateway.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: send_run_result 补充实现 (P1-14)**

找到 Task 8.1 或 8.3 中 `send_run_result()` 的空壳定义，补充完整实现指导：

```markdown

> **send_run_result() 实现 (P1)**：Run 完成时，从 Run 结果中提取最终文本消息，格式化为 IM 消息格式（纯文本 + 可选 Markdown），调用对应 adapter.send() 发送到 IM 渠道。实现要点：
> 1. 从 `run.messages` 中提取最后一条 `role=assistant` 的文本内容
> 2. 如果文本超过 IM 平台消息长度限制（飞书 4000 字、企微 2048 字、Telegram 4096 字），截断并附加"完整结果请在 Web 端查看"
> 3. 如果 Run 状态为 `failed`，发送格式化的错误信息
> 4. 调用 `adapter.send(chat_id=session.im_chat_id, content=formatted_message)`
```

- [ ] **Step 3: 配对码改进 (P1-15)**

找到 Task 8.3 中配对码相关描述，修改：

```markdown

> **配对码改进 (P1)**：
> 1. **碰撞重试**：配对码生成时检查是否已存在相同有效码，存在则重新生成（最多 3 次重试）
> 2. **触发方式**：改用前缀命令 `/pair 123456` 触发配对，避免 6 位纯数字在正常对话中误触发。配对码仍为 6 位数字，但必须通过 `/pair` 命令提交。
```

- [ ] **Step 4: Commit**

```bash
git add PRD/DOC-08-Backend-IM-Gateway.md
git commit -m "docs(DOC-08): v3.1 — implement send_run_result, fix pairing code collision and trigger"
```

---

### Task 10: DOC-09 — 新增 Task 9.3 + 审计修补

**Files:**
- Modify: `PRD/DOC-09-Backend-MCP-Provider-Admin.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: Task 9.1 MCP user_id 修复 (P1-16)**

在 Task 9.1 Part A 中补充：

```markdown

> **数据隔离修复 (P1)**：`list_servers()`, `delete_server()`, `create_server()` 所有方法必须接受 `user_id` 参数并强制按 `user_id` 过滤，确保铁律 4（数据隔离）。
```

- [ ] **Step 3: Task 9.1 MCP API 层补充 (P0-7)**

在 Task 9.1 Part A 或 Part B 中补充 MCP API 路由文件的实现指导：

```markdown

> **MCP API 层 (P0)**：Task 9.1 Part B 必须包含完整的 `app/api/v1/mcp.py` 路由文件实现指导，包括：
> - `POST /mcp-servers` — 创建 MCP Server 配置
> - `GET /mcp-servers` — 列出当前用户的 MCP Server
> - `DELETE /mcp-servers/{id}` — 删除 MCP Server
> - `POST /mcp-servers/{id}/test` — 测试 MCP Server 连通性
> - 所有端点强制 `user_id` 数据隔离
```

- [ ] **Step 4: 新增 Task 9.3 — Admin 审计日志与系统管理**

在文件末尾追加：

```markdown

---

## Task 9.3: Admin 审计日志与系统管理

### Part A — 设计与解释

#### 问题陈述

DOC-09 标题包含"Admin"但缺少 Admin 管理功能的 Task。需要实现管理员审计日志查询和系统管理接口。

#### API 端点

```
GET    /admin/audit-logs          — 查询审计日志（分页、筛选）
GET    /admin/audit-logs/export   — 导出审计日志（CSV/JSON）
GET    /admin/system/stats        — 系统统计（用户数、会话数、Run 数、Token 用量）
GET    /admin/users               — 用户列表管理
PATCH  /admin/users/{id}/role     — 修改用户角色
DELETE /admin/users/{id}          — 禁用用户（软删除）
```

#### 审计日志查询参数

```python
class AuditLogQuery:
    action: str | None = None       # 筛选操作类型（如 "harness.guardrail_trigger"）
    user_id: str | None = None      # 筛选用户
    start_time: str | None = None   # ISO 8601 起始时间
    end_time: str | None = None     # ISO 8601 结束时间
    page: int = 1
    page_size: int = 50             # max 200
```

#### 验收标准

- `GET /admin/audit-logs` 返回分页的审计日志
- 非 admin 用户调用返回 403
- 审计日志包含 Harness 治理事件（guardrail_trigger、permission_deny 等）
- 系统统计数据准确

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充

## 完成后

1. Admin 审计日志查询和系统管理接口正常工作
2. 更新 PROGRESS.md：Task 9.3 状态标记为 done
```

- [ ] **Step 5: 目录更新**

确保 Task 9.3 出现在文件开头的目录中。

- [ ] **Step 6: Commit**

```bash
git add PRD/DOC-09-Backend-MCP-Provider-Admin.md
git commit -m "docs(DOC-09): v3.1 — add Task 9.3 Admin audit log, fix MCP user_id isolation and API layer"
```

---

### Task 11: DOC-10 — 审计修补

**Files:**
- Modify: `PRD/DOC-10-Frontend-Foundation.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: SSE 事件数修正 (P1-17)**

在 Task 10.2 Part A 的"SSE 事件处理矩阵"中，确认事件总数为 **12 种**（与 DOC-01 §7.1 对齐）。如果矩阵中缺少 `session_title` 事件，补充：

```markdown
| `session_title` | 更新当前会话的标题显示 | 更新 SessionStore 的 title 字段 |
```

- [ ] **Step 3: harness_event 子类型完善 (P1-18)**

在 Task 10.2 Part A 的 `harness_event subtype 展示策略` 中，确保列出完整 8 种子类型：

```markdown
| 子类型 | 展示方式 |
|--------|---------|
| `guardrail_trigger` | Toast 警告 + 详情面板高亮 |
| `permission_deny` | Toast 通知 |
| `permission_ask` | 弹窗请求用户确认 |
| `loop_detected` | Toast 提示 + 循环计数 |
| `compaction` | 静默通知（底部状态栏） |
| `circuit_break` | Toast 错误 + 重试建议 |
| `feedback_alert` | Toast 警告 + 链接到仪表盘 |
| `middleware_action` | 静默通知（调试面板可见） |
```

- [ ] **Step 4: Commit**

```bash
git add PRD/DOC-10-Frontend-Foundation.md
git commit -m "docs(DOC-10): v3.1 — fix SSE event count to 12, complete harness_event 8 subtypes"
```

---

### Task 12: DOC-11 — Skills Store UI (Task 11.5) + 审计修补

**Files:**
- Modify: `PRD/DOC-11-Frontend-Features.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: Task 11.2 规格补充 (P1-19)**

在 Task 11.2 Part A 中，当前只有验收标准没有问题陈述。补充：

```markdown

#### 问题陈述

用户需要能够浏览、搜索历史会话，并快速切换到已有会话。当前 Part A 缺少组件接口定义和搜索策略。

#### 组件接口

- `SessionListPanel` — 左侧会话列表（支持搜索 + 分页加载）
- `SessionSearchBar` — 搜索栏（按标题全文搜索，debounce 300ms）
- `SessionCard` — 会话卡片（标题、最后消息预览、时间、未读标记）

#### 搜索策略

- 前端：TanStack Query 的 `useInfiniteQuery` 分页 + 搜索防抖
- 后端：`GET /sessions?q=...&page=...&page_size=20`，标题 ILIKE 搜索
```

- [ ] **Step 3: Task 11.3 规格补充 (P1-19)**

在 Task 11.3 Part A 中补充：

```markdown

#### 问题陈述

设置页面需要管理 Provider、MCP Server、IM 绑定三类配置。当前 Part A 缺少表单校验规范。

#### 表单校验规范

- Provider 表单：API Key 必填 + 格式校验（sk- 前缀）、Base URL 格式校验、连通性测试按钮
- MCP Server 表单：Name 唯一性校验、Command 必填、JSON Schema 校验 args
- IM 绑定：配对码 6 位数字校验、绑定状态实时刷新
```

- [ ] **Step 4: 全部 Task 补充状态 UI (P1-20)**

在 Task 11.1~11.4 的每个 Part A 中，补充统一的状态 UI 规范：

```markdown

#### 状态 UI 规范

- **Loading 态**：骨架屏（Skeleton）替代 spinner，保持布局稳定
- **Empty 态**：居中插图 + 引导文案 + 操作按钮（如"创建第一个会话"）
- **Error 态**：内联错误提示 + 重试按钮，不使用全屏错误页
- **Offline 态**：顶部横幅提示"网络连接已断开"，灰色覆盖交互区域
```

- [ ] **Step 5: 新增 Task 11.5 — Skills Store & Plugin Builder UI**

在文件末尾追加。内容来源：Spec §6。

```markdown

---

## Task 11.5: Skills Store & Plugin Builder UI

### Part A — 设计与解释

#### 问题陈述

Skills 市场和插件创建流程需要前端界面支持。包括 Skills 商店页面（/skills）、插件创建向导（/plugins/create）、Harness 配置页（/settings/harness）。

#### Skills 商店页面 (`/skills`)

```
┌─────────────────────────────────────────────────────┐
│ Skills 市场                                          │
├─────────────────────────────────────────────────────┤
│ [搜索框]  [源筛选: 全部|Manus|npm|GitHub|已安装]       │
├─────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ Skill A  │  │ Skill B  │  │ Skill C  │          │
│  │ 描述...  │  │ 描述...  │  │ 描述...  │          │
│  │ @manus   │  │ @npm     │  │ @github  │          │
│  │ [安装]   │  │ [已安装] │  │ [安装]   │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
```

**组件清单**：

```
src/features/skills/
├── SkillsStorePage.tsx
├── SkillSearchBar.tsx
├── SkillCard.tsx
├── SkillGrid.tsx
├── SkillDetailDrawer.tsx
├── InstallConfirmDialog.tsx
├── InstalledSkillsList.tsx
├── use-skills.ts
└── skills-api.ts
```

#### 插件创建向导 (`/plugins/create`)

左右分栏：左侧对话面板（PluginBuilder Agent），右侧实时预览（目录结构 + 阶段指示器）。

```
src/features/plugins/
├── PluginCreatePage.tsx
├── PluginChatPanel.tsx
├── PluginPreviewPanel.tsx
├── PluginPhaseIndicator.tsx
├── PluginStructureTree.tsx
├── ConfirmDesignButton.tsx
├── use-plugin-builder.ts
└── plugin-builder-api.ts
```

#### Harness 配置页 (`/settings/harness`)

```
src/features/settings/
├── HarnessConfigPage.tsx
├── GuardrailRulesList.tsx
├── MiddlewareTogglePanel.tsx
├── PermissionPolicyEditor.tsx
├── ConfigSourceTrace.tsx
└── use-harness-config.ts
```

#### 状态 UI 规范

- **Loading 态**：骨架屏
- **Empty 态**：居中引导（"还没有安装任何 Skill，去市场看看？"）
- **Error 态**：内联错误 + 重试按钮
- **Offline 态**：顶部横幅

#### 验收标准

- Skills 商店：搜索、安装、卸载流程完整走通（Playwright E2E，桌面 + 移动）
- 插件创建向导：完整多轮对话 → 确认 → 生成流程
- Harness 配置页：修改 Guardrail 规则后立即生效
- 正常流程 + 边界场景（搜索无结果、安装失败、网络错误）全覆盖

### Part B — Claude Code 执行 Prompt

> 待实施计划执行阶段补充

## 完成后

1. Skills 商店、插件创建向导、Harness 配置页正常工作
2. Playwright E2E 桌面 + 移动端全部通过
3. 更新 PROGRESS.md：Task 11.5 状态标记为 done
```

- [ ] **Step 6: 目录更新**

确保 Task 11.5 出现在目录中。

- [ ] **Step 7: Commit**

```bash
git add PRD/DOC-11-Frontend-Features.md
git commit -m "docs(DOC-11): v3.1 — add Task 11.5 Skills Store UI, supplement Task 11.2/11.3 specs, add state UI standards"
```

---

### Task 13: DOC-12 — Entropy 修复 + ADR 重编号

**Files:**
- Modify: `PRD/DOC-12-Observability-and-Entropy.md`

- [ ] **Step 1: 版本号升级**

第 4 行 `3.0` → `3.1`

- [ ] **Step 2: ADR 重编号**

全文替换：
1. `ADR-023` → `ADR-032`（Entropy Detection 半自动模式）
2. `ADR-022` → `ADR-031`（TokenEstimator 策略模式）

先从大编号开始替换。

- [ ] **Step 3: Entropy 窗口修复 (P0-8)**

在 Task 12.2 Part A 中，找到 Entropy Detection 的窗口计算描述。当前 `previous = aggregate(days=14)` 与 `current = aggregate(days=7)` 重叠。修复为：

```markdown

> **窗口修复 (P0)**：`HarnessAnalytics.aggregate()` 增加 `offset_days: int = 0` 参数。EntropyDetector 使用非重叠窗口：
> ```python
> current = analytics.aggregate(days=7, offset_days=0)   # 最近 7 天
> previous = analytics.aggregate(days=7, offset_days=7)   # 7~14 天前
> ```
> 两个窗口不重叠，delta 计算才有意义。
```

同时在 Part B 代码中，找到 `aggregate(days=14)` 并修正为 `aggregate(days=7, offset_days=7)`。

- [ ] **Step 4: Entropy 阈值可配置 (P1-21)**

在 Task 12.2 Part A 的阈值定义处补充：

```markdown

> **阈值校准说明**：以下阈值为初始估计值，通过环境变量可配置（`ENTROPY_THRESHOLD_*`），上线后根据实际数据校准。
```

- [ ] **Step 5: ResourceMonitor 定时调度 (P2-8)**

在 Task 12.3 Part A 补充：

```markdown

> **定时调度**：ResourceMonitor 通过 `asyncio` 后台任务定时运行（间隔通过 `RESOURCE_MONITOR_INTERVAL` 环境变量配置，默认 60 秒）。在 FastAPI `lifespan` 中启动和停止。
```

- [ ] **Step 6: TiktokenEstimator 局限性标注 (P2-7)**

在 Task 12.1 Part A 的 TokenEstimator 描述中补充：

```markdown

> **局限性说明**：TiktokenEstimator 基于 OpenAI 的 tiktoken 库，对 Claude 模型的 token 计数存在约 5-15% 的偏差。这是已知局限，Phase 1 可接受。Phase 2 可通过接入 Anthropic 的 token counting API 提升精度。
```

- [ ] **Step 7: Commit**

```bash
git add PRD/DOC-12-Observability-and-Entropy.md
git commit -m "docs(DOC-12): v3.1 — fix entropy window overlap (P0), renumber ADRs, add threshold config"
```

---

### Task 14: DOC-AUDIT-00-04 — 标记已整合

**Files:**
- Modify: `PRD/DOC-AUDIT-00-04.md`

- [ ] **Step 1: 标记已整合**

在文件开头的元数据区域，添加：

```markdown
> **状态**: ✅ 已整合到 PRD v3.1 修订中（2026-04-05）
> **整合文档**: `docs/superpowers/specs/2026-04-05-prism-v2-prd-v3.1-revision-design.md`
```

- [ ] **Step 2: Commit**

```bash
git add PRD/DOC-AUDIT-00-04.md
git commit -m "docs(AUDIT): mark DOC-AUDIT-00-04 as integrated into v3.1"
```

---

### Task 15: 最终验证

- [ ] **Step 1: 版本号全检**

检查所有 13 个 DOC 文件的第 4 行，确认版本号均为 `3.1`。

```bash
grep -n "版本" PRD/DOC-*.md | head -20
```

- [ ] **Step 2: ADR 编号冲突检查**

确认 DOC-05 和 DOC-06 不再有 ADR 编号冲突：

```bash
grep -rn "ADR-" PRD/DOC-*.md | sort -t'-' -k2 -n
```

预期：ADR-001~032，无重复。

- [ ] **Step 3: 新 Task 存在性检查**

确认 7 个新 Task 都存在：

```bash
grep -l "Task 3.6" PRD/DOC-03-*.md
grep -l "Task 4.5" PRD/DOC-04-*.md
grep -l "Task 5.5" PRD/DOC-05-*.md
grep -l "Task 5.6" PRD/DOC-05-*.md
grep -l "Task 5.7" PRD/DOC-05-*.md
grep -l "Task 9.3" PRD/DOC-09-*.md
grep -l "Task 11.5" PRD/DOC-11-*.md
```

- [ ] **Step 4: Commit 最终标记**

```bash
git add -A
git commit -m "docs: PRD v3.1 revision complete — 7 new tasks, 39 audit fixes, ADR renumbering"
```
